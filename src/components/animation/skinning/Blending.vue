<template>
  
</template>

<script lang="ts">
// 找个时间解决下 dat.gui 跟 lil.gui 的问题
// import bleadings  from "@/other-ts/three-other-webgl/animation/skinning/blending";
export default {
    setup() {
        // const Bleadings = new bleadings();
        // Bleadings.createMesh();
        // Bleadings.createGeometry();
        // Bleadings.createCamera();
        // Bleadings.createRender();
        // Bleadings.createLight();
        // Bleadings.createListen();
    },
}
</script>

<style>

</style>